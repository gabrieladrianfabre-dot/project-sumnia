# One-click deploy on Render (https://render.com).
# New > Blueprint > connect this repo, then set ADMIN_PASSWORD when prompted.
services:
  - type: web
    name: sumnia
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: ADMIN_PASSWORD
        sync: false # you enter the curator password in the Render dashboard
      - key: DATABASE_URL
        sync: false # Postgres connection string (e.g. Supabase); omit to use the ephemeral JSON store
      - key: NODE_VERSION
        value: "20"
